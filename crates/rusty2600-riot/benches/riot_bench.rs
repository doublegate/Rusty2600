//! Criterion bench for `rusty2600-riot` (each chip is benchmarkable in isolation —
//! that is what the one-directional crate graph buys us).
#[allow(clippy::missing_const_for_fn)] // becomes a real criterion harness.
fn main() {
    // TODO: criterion::criterion_group!/main! once the chip has a tick to measure.
}
