# Rust
/target/
**/target/
**/*.rs.bk
*.pdb
Cargo.lock.bak

# IDE
.idea/
.vscode/
*.iml
*.swp
*~
.DS_Store

# Generated
*.profraw
*.profdata
/coverage/
/perf.data*
/flamegraph.svg

# Per-developer
.envrc
.direnv/

# rusty-console-forge additions
/ref-proj/
/tests/captures/
/perf-logs/
# The wasm frontend's Trunk project lives at crates/rusty2600-frontend/web/
# (not a repo-root web/), so this must NOT be root-anchored (the previous
# `/web/dist/` pattern never actually matched anything — there is no
# repo-root web/ dir — and this local build output was untracked by luck).
**/web/dist/

# Test ROMs and Golden Logs (commercial ROMs are never committed; the CC0/MIT
# SingleStepTests/Klaus corpora ARE committed and live under tests/cpu-timing/
# and tests/roms/ outside these paths)
/tests/roms/Klaus2m5/
/tests/golden/ProcessorTests/
/tests/roms/stella/
/tests/roms/external/
